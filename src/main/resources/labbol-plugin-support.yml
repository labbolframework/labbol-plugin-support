labbol:
 plugin:
  support:
   #是否开启权限验证。默认值false
   rights: true
   #是否开启日志记录。默认值false
   logRecord: true
   #是否开启xss注入过滤器。默认值false
   xss: true
   #是否开启安全过滤器。默认值false
   security: true
   #是否开启文件上传控制（ip段）
   fileUploadSCIP: true